// For Of Loop in JavaScript

// The `for...of` loop provides an elegant way to iterate through array elements. It is a modern alternative to the traditional `for` loop.


const numbers = [1, 2, 3, 4, 5];
// Iterate over the elements
console.log("Iterating over the array:");

for (let number of numbers) {
    console.log(number);
}

